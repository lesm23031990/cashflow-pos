# JWT — OBLIGATORIO. Genera uno real con: openssl rand -hex 32
# Nunca subas un secreto real a Git. Este archivo es una plantilla publica.
JWT_SECRET=change-me-run-openssl-rand-hex-32

# Base de datos (opcional, default: ./data/precios.db)
# DB_PATH=./data/precios.db

# Plane.so API — https://plane.so
PLANE_API_KEY=your_api_key_here
PLANE_WORKSPACE=your_workspace_slug
PLANE_PROJECT=your_project_id
